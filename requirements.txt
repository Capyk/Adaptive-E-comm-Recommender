# Core Data Handling and Numerical Operations
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0

# Recommendation Modeling (Surprise Library for easy Collaborative Filtering)
scikit-surprise>=1.1.3

# MLOps & Experiment Tracking
mlflow>=2.9.0  # Crucial for logging models, parameters, and metrics
tqdm>=4.66.1   # For progress bars during long training/data processing

# Hyperparameter Optimization (for Card 19)
optuna>=3.4.0

# API Deployment & Serving (FastAPI Stack)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0  # ASGI server to run FastAPI
pydantic>=2.5.0           # Data validation (used heavily by FastAPI)

# Database Connector (For the simulated SQLite database)
SQLAlchemy>=2.0.0 # ORM/toolkit for database interaction
psycopg2-binary>=2.9.9 # Recommended if you switch to PostgreSQL later

# Testing (for Card 18)
pytest>=7.4.3
pytest-cov>=4.1.0 # For measuring code coverage during CI/CD